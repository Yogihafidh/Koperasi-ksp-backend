name: Deploy to Production

on:
  push:
    branches:
      - production

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build image
        run: |
          docker build -t ghcr.io/${{ github.repository_owner }}/ksp-api:latest .

      - name: Push image
        run: |
          docker push ghcr.io/${{ github.repository_owner }}/ksp-api:latest

      - name: Deploy to server
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: 146.190.111.184
          username: root
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            docker pull ghcr.io/${{ github.repository_owner }}/ksp-api:latest
            docker stop koperasi_api || true
            docker rm koperasi_api || true
            docker run -d \
              --name koperasi_api \
              --network ksp_koperasi_network \
              --env-file /var/www/ksp/.env.production \
              -p 3000:3000 \
              ghcr.io/${{ github.repository_owner }}/ksp-api:latest
